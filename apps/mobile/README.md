# @eddy/mobile

Expo + React Native client for Eddy.

## Status

Not yet scaffolded. Placeholder during Week 1 monorepo restructure.

## Next step (Week 2)

From the repo root:

```bash
npx create-expo-app@latest apps/mobile --template blank-typescript --no-install
```

Then merge the generated files into this directory and run `npm install` at the repo root so the workspace links resolve correctly.
