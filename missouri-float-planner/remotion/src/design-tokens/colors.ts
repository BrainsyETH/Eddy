// The palette is owned by shared/social-brand.ts — the same object the Next.js
// OG covers import — so a reel and its cover can never disagree on a hex.
export { colors, semanticColors } from "../../../shared/social-brand";
