node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
ios/
android/

# Local secrets. EXPO_PUBLIC_MAPBOX_TOKEN lives here; EAS supplies it in builds.
.env
# Every dotenv variant, not just the two that were listed. `eas env:pull`
# writes a real one of these full of live keys, and the name it picks depends
# on the flags — .env.production was tracked until this line existed.
.env.*
# The template is the one that must stay, since it is the documentation.
!.env.example

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Editor swap files. One was committed to main by accident.
*.swp
*.swo

# Python bytecode from the helper scripts in scripts/.
__pycache__/
*.pyc
