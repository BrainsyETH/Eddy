import { Placeholder } from '@/components/Placeholder';

export default function ProfileScreen() {
  return (
    <Placeholder
      title="Profile"
      blurb="Account, subscription status and notification preferences."
      waitingOn="Sign in with Apple + RevenueCat SDK"
    />
  );
}
