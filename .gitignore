# Operating-system noise. Application-specific ignores live in each
# project's own .gitignore (missouri-float-planner/, eddy-ios/,
# clipengine-local/). NOTE: .easignore does NOT read this file — it is an
# independent allowlist; see its header.
.DS_Store
._*
Thumbs.db
Desktop.ini
