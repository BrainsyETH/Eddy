# PR 1295/1296 follow-ups

Saved trips retain only known static road-access warnings, with the same filtering applied when old offline records are displayed. Live water verdicts and stale-reading warnings are excluded.

Dangerous water rows remain visible during every Today snooze option. Other banners can still be snoozed.

Premium Read keeps the previous successful result during focus/foreground refresh, coalesces requests, cancels inactive requests, and refreshes at most once per minute unless explicitly retried. Authorization failures clear protected content.

The Remotion workflow installs ffmpeg so health, render smoke, and visual comparison can all run. Visual baseline changes require manual inspection; a render passing is not visual approval.

No route-estimation formula or long-route calibration changes are included.
